---
epic: 2
story: 1
title: "Implement 'Load More' Pagination with URL-Based Termination"
status: "Ready for Review"
---

### Story

*As a Crawler Agent, I want to repeatedly click a 'load more' button to reveal more content, but stop immediately if I encounter an article I've already saved, so that I can efficiently gather all new content.*

### Acceptance Criteria

1. The CSS selector for the "load more" button is configurable in `config.yaml`.
2. The crawler enters a loop, clicking the "load more" button at the end of each cycle.
3. In each loop, the crawler checks every article link's URL against the database.
4. If a URL is found that already exists in the database, the entire crawl process stops immediately and logs a "Crawl complete" message.
5. If the "load more" button cannot be found, the crawl process also stops and logs a "Crawl complete" message.

### Dev Notes

*   **Primary Goal:** This story is focused on implementing the core pagination and termination loop. *(Source: docs/prd.md - FR9)*
*   **Database Check is Key:** The most critical part of this logic is the `url_exists` check against the database for each link. This is the sole condition for terminating the crawl. *(Source: docs/architecture.md#5-core-logic-and-workflow)*
*   **Configuration:** The `load_more_selector` must be loaded from `config.yaml`. *(Source: docs/prd.md - NFR3)*

### Tasks / Subtasks

1.  **[x] (AC: 1)** Add `load_more_selector` to `config.yaml`.
2.  **[x] (AC: 3)** In `src/database/persistence.py`, create a new function `url_exists(url: str) -> bool`.
3.  **[x] (AC: 2, 3, 4, 5)** In `src/core/crawler.py`, refactor the `run_crawl` function to implement the main pagination loop.
4.  **[x] (Loop Logic)** Inside the loop, get all content links from the current page view.
5.  **[x] (Loop Logic)** For each link, call the `url_exists` function.
6.  **[x] (AC: 4)** If `url_exists` returns `True`, break all loops and end the crawl.
7.  **[x] (AC: 2, 5)** If the loop completes without finding any existing URLs, find and click the "load more" button. If the button is not found, end the crawl.

### Dev Agent Record
*   **Agent Model Used:** 
*   **Debug Log References:**
*   **Completion Notes:**
*   **File List:**
    *   `config/config.yaml`
    *   `src/database/persistence.py`
    *   `tests/test_persistence.py`
    *   `src/core/crawler.py`
*   **Change Log:**
    *   (James) Added `load_more_selector` to config file.
    *   (James) Confirmed `url_exists` function is implemented.
    *   (James) Added unit test for `url_exists` function.
    *   (James) Refactored `run_crawl` to implement new pagination logic.
    *   (James) Refactored `browser_utils` to remove redundant pagination logic.
